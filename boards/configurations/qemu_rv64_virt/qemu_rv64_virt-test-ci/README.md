QEMU RISC-V 64 bit `virt` Configuration Board for CI
====================================================

This board crate targets the QEMU RISC-V 64 bit `virt` platform.
It is designed to support running tests in CI.
