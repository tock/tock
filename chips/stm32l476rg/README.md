# ST Micro stm32l476rg MCU

Builds upon the `stm32l4xx` crate and includes hardware setup that is specific to this version of
the stm32l4xx series. Boards that include an stm32l476rg MCU should use this crate as a dependency
and not the `stm32l4xx` crate.
