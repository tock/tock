# Tock Registers Procedural Macros

This is the procedural macro crate for `tock-registers`. For documentation, see
`tock-registers`' README at `../README.md`, its doc directory at `../doc`, and
`tock-registers`' rustdoc.
