QEMU RISC-V 32 bit `virt` Configuration Board for CI Testing
========================================================================

This board crate targets the QEMU RISC-V 32 bit `virt` platform. It includes
capsules useful for testing apps in CI.